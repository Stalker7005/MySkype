cmake_minimum_required(VERSION 3.0)
project(MySkype)

include(CmakeConstants.cmake)
include(CmakeUtilsFunctions.cmake)

set(EXECUTABLE_OUTPUT_PATH ${CMAKE_BINARY_DIR}/bin)
set(LIBRARY_OUTPUT_PATH ${CMAKE_BINARY_DIR}/bin)
set(CMAKE_VERBOSE_MAKEFILE true)
  
if(WIN32)

add_definitions(-DUNICODE -D_UNICODE)
add_definitions(-DBUILD_WINDOWS)

message(STATUS "Configuration for Windows...")
if(MSVC)
add_definitions(-DNANODBC_USE_UNICODE -DNANODBC_ODBC_VERSION=SQL_OV_ODBC3)
set(CMAKE_CXX_FLAGS ${CMAKE_CXX_FLAGS} -DNANODBC_ODBC_VERSION=SQL_OV_ODBC3)
#remove_definitions(-DSQL_ATTR_ASYNC_STMT_EVENT -DSQL_API_SQLCOMPLETEASYNC)
#set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -std=c++14)

	message(STATUS "Set static runtime...")
	#set static runtime
	FOREACH(flag_var CMAKE_CXX_FLAGS CMAKE_CXX_FLAGS_DEBUG CMAKE_CXX_FLAGS_RELEASE CMAKE_CXX_FLAGS_MINSIZEREL CMAKE_CXX_FLAGS_RELWITHDEBINFO
			CMAKE_C_FLAGS CMAKE_C_FLAGS_DEBUG CMAKE_C_FLAGS_RELEASE CMAKE_C_FLAGS_MINSIZEREL CMAKE_C_FLAGS_RELWITHDEBINFO)
			if(${flag_var} MATCHES "/MD")
			STRING(REGEX REPLACE "/MD" "/MT" ${flag_var} "${${flag_var}}")
			endif(${flag_var} MATCHES "/MD")
			if(${flag_var} MATCHES "/MDd")
			STRING(REGEX REPLACE "/MDd" "MTd" ${flag_var} "${${flag_var}}")
			endif(${flag_var} MATCHES "/MDd")
	ENDFOREACH(flag_var)
	
message("Looking for boost...")

set(Boost_USE_STATIC_LIBS        ON) # only find static libs
set(Boost_USE_MULTITHREADED      ON)
set(Boost_USE_STATIC_RUNTIME    ON)
set(BOOST_ROOT  "${BUILD_FILES_DIR}\\boost_1_60_0")
set(BOOST_LIBRARYDIR ${BOOST_ROOT}\\stage\\lib)
find_package(Boost 1.60.0 COMPONENTS system date_time regex thread chrono filesystem REQUIRED)

Assert(Boost_FOUND)

addLibInclude("cereal")
addLibInclude("log4cplus")

#addLibLink("log4cplus" 0 log4cplusU.lib)
addLibLink("log4cplus" 1 log4cplusSUD.lib)


#message(FATAL_ERROR "Log 4 cplus libs:${LOG4CPLUS_LIB_DIRS}")

endif(MSVC)
endif(WIN32)

if(UNIX)
add_definitions(-DBUILD_LINUX)
endif(UNIX)

include_directories(${CEREAL_INCLUDE})
add_subdirectory(ServerManagers)
add_subdirectory(ServerObjects)
add_subdirectory(ODBCHelperLib)
add_subdirectory(Tests)
add_subdirectory(BaseObjects)
add_subdirectory(ThreadsUtilsLib)
add_subdirectory(Client)
add_subdirectory(Server)
add_subdirectory(NetworkLib)
add_subdirectory(SerealizationUtilsLib)

